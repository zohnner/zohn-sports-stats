# Secrets — never commit
.env
.env.*
*.pem
*.key

# Wrangler local state
.wrangler/
worker/.wrangler/

# Node / npm (only needed for wrangler deploy)
node_modules/
package-lock.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
*.swp

# Meta / prompting files — not project code
reflection.md
.claude/

# Owner-private session checklists — contain operational security detail
owner-checklist-*.md

# Local scratch / working notes — not deployed, not project code
AUDIT-*.md
INQUISITION_RESPONSES.md
deep-review-*.md
design-review-*.md
*-tokens-draft.css
assets/_svgpreview.png
ZDevNotes
X Post/
